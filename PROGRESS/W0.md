# W0 环境搭建(2026-07-03 完成)

## 本周做完的事

- 和 Claude 敲定 6 周项目 plan,写到 `PLAN.md` 里(自包含,可切模型)
- 决策:主项目 Mini-Redis(Java + Netty);拓展 A 主从复制、拓展 B 类 XPipe Keeper 押后
- 决策:AI 使用政策分双轨 —— RESP 协议 / Netty 主循环 / 主从复制 / AOF 重写必须亲手写;命令实现 / 单测 / 脚本可 AI 辅助
- Git 身份分家:
  - global 设 GitHub noreply `218736830+0G-C@users.noreply.github.com`
  - XPipe 仓库 local 保持 QQ 邮箱(之前已经用过,统一延续)
  - Beacon 仓库 local 设公司邮箱 `yihaohuang@trip.com`
- 环境升级:
  - 装 PowerShell 7(为了 `&&`);IDEA Terminal Shell path → `pwsh.exe`
  - `git config --global init.defaultBranch main`(以后新建不再默认 master)
- 建 Maven 工程 `io.github.ogc.miniredis:mini-redis`
  - Netty 4.1.108.Final / Lombok 1.18.32 / Logback 1.5.6 / JUnit 5.10.2 / AssertJ 3.25.3 / Mockito 5.11.0 / JMH 1.37
  - pom.xml + `.gitignore` + `logback.xml` + `RedisServer` 骨架
  - Run 后控制台和 `logs/mini-redis.log` 都有 `mini-redis booting` 日志
- 首次 push:3 个 commit 上 main
  - `Initial commit`(GitHub 建仓库自带)
  - `chore: init maven project with jdk 17`
  - `feat: bootstrap redis server with logback`
- 建 orphan 分支 `journal`,存 PLAN + PROGRESS

## 每日 log

### 2026-07-03(周五)@ 公司 Windows

密集环境搭建 + 决策日,基本上按上面"本周做完的事"顺序推进。

**踩过的坑(面试可讲)**
- IDEA Ctrl+F12 无响应(公司电脑快捷键被截图工具/亮度键占) → 换用 psvm + 自动补全验证 Lombok 生效
- PowerShell 5 不支持 `&&`,只能用 `;` 或 `if ($?) { ... }`;升级 PS7 后还要改 IDEA Terminal Shell path,不然 IDE 里还是老壳
- 首次 push 报 `src refspec main does not match any` —— 本地默认分支是 master,`git branch -M main` 重命名后成功
- 差点全局覆盖 Beacon/XPipe 的 GitLab 邮箱(local 空,靠 global 读) —— 及时 `git config --list --local` 检查,避免下次公司 PR 出现 noreply 邮箱

## 未解决 / 遗留

- 居家 Windows 环境:WSL2、JDK 17、IDEA、redis-cli 都还没装(周六一起做)
- Mac 不装 IDEA Settings Sync(Mac 只跑实习任务,不动 mini-redis)
- 简历卖点里"XPipe PR #1053 CONTRIBUTOR"素材已有,待 W6 定稿一起打磨
